#include <stdio.h>
#include <locale.h>
#include <string.h>

void main(){
    setlocale(LC_ALL, "Portuguese");

    /*
    char nome[10];
    strcpy(nome, "Matheus");
    printf("Seu nome é %s", nome);
    */

    /*
    char nome[40] = "Matheus e Jessica Peixinho";
    int num = strlen(nome);
    printf("O nome %s tem %i letras", nome, num);
    */

    char nome[20] = "Matheus";
    strcat()
}
